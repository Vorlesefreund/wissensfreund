import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('placeholder smoke test', (WidgetTester tester) async {
    // Placeholder — actual integration tests run on device.
    expect(1 + 1, equals(2));
  });
}
