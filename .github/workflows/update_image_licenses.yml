name: Update Media Assets

# Runs on the 1st of every month at 03:00 UTC, and on manual trigger.
#
# Two independent pipelines run in parallel:
#
#   Image pipeline (fast, ~30 min total):
#     prepare-images → images (3 shards) → images-merge
#
#   Audio pipeline (slow, up to 90 min):
#     prepare-audio  → download
#
# Image jobs never wait for audio.
# If audio discovery times out, images still complete successfully.
#
# Manual test run with limited articles:
#   Go to Actions → Update Media Assets → Run workflow
#   Set "max_articles" to e.g. 10

on:
  schedule:
    - cron: '0 3 1 * *'
  workflow_dispatch:
    inputs:
      max_articles:
        description: 'Max HTML articles to scan for audio (0 = all, 10 = quick test)'
        required: false
        default: '0'
        type: string

env:
  ZIM_VERSION: klexikon_de_all_maxi_2026-05
  ZIM_URL: https://ftp.fau.de/kiwix/zim/other/klexikon_de_all_maxi_2026-05.zim

jobs:
  # ── IMAGE PIPELINE ─────────────────────────────────────────────────────────────
  #
  # prepare-images: ZIM download + image map + media_licenses.json + publish
  # Runtime: ~20 min (ZIM download ~5 min, image map ~10 min, rest <5 min)

  prepare-images:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: '3.10'

      - name: Install Python dependencies
        run: pip install requests zstandard

      - name: Download ZIM file
        run: |
          echo "Downloading $ZIM_URL"
          curl -fsSL -o klexikon.zim "$ZIM_URL"
          ls -lh klexikon.zim

      # ── Optional diagnostic (only on test runs) ───────────────────────────
      - name: Dump article HTML structure (diagnostic)
        if: ${{ inputs.max_articles != '0' }}
        run: python scripts/dump_article_html.py
        env:
          ZIM_FILE: klexikon.zim
          ARTICLE: Beethoven
          CONTEXT: '1500'

      # ── Build hash→Commons filename map ──────────────────────────────────
      - name: Build image map (hash → Commons filename)
        run: python scripts/build_image_map.py
        env:
          ZIM_FILE:     klexikon.zim
          MAX_ARTICLES: ${{ inputs.max_articles || '0' }}

      # ── Generate media_licenses.json ──────────────────────────────────────
      - name: Generate media_licenses.json
        run: python scripts/generate_license_json.py
        env:
          ZIM_FILE:       klexikon.zim
          ZIM_VERSION:    ${{ env.ZIM_VERSION }}
          IMAGE_MAP_FILE: image_map.json

      # ── Upload artifacts for downstream image jobs ────────────────────────
      - name: Upload ZIM artifact
        uses: actions/upload-artifact@v7
        with:
          name: klexikon-zim
          path: klexikon.zim
          retention-days: 1

      - name: Upload licenses artifact
        uses: actions/upload-artifact@v7
        with:
          name: media-licenses
          path: media_licenses.json
          retention-days: 1

      # ── Publish image data to R2 and GitHub Release ───────────────────────
      - name: Create zim_version.json
        run: |
          ZIM_SIZE=$(stat -c%s klexikon.zim 2>/dev/null || echo 0)
          TODAY=$(date -u +%Y-%m-%d)
          cat > zim_version.json <<EOF
          {
            "version": "$ZIM_VERSION",
            "zim_url": "$ZIM_URL",
            "size_bytes": $ZIM_SIZE,
            "updated": "$TODAY"
          }
          EOF
          echo "zim_version.json:"
          cat zim_version.json

      - name: Upload zim_version.json to Cloudflare R2
        run: |
          aws s3 cp zim_version.json \
            s3://${{ secrets.CLOUDFLARE_R2_BUCKET_NAME }}/zim_version.json \
            --endpoint-url https://${{ secrets.CLOUDFLARE_ACCOUNT_ID }}.eu.r2.cloudflarestorage.com \
            --content-type application/json \
            --cache-control "public, max-age=3600"
        env:
          AWS_ACCESS_KEY_ID:     ${{ secrets.CLOUDFLARE_R2_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.CLOUDFLARE_R2_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION:    auto

      - name: Upload media_licenses.json to Cloudflare R2
        run: |
          aws s3 cp media_licenses.json \
            s3://${{ secrets.CLOUDFLARE_R2_BUCKET_NAME }}/media_licenses.json \
            --endpoint-url https://${{ secrets.CLOUDFLARE_ACCOUNT_ID }}.eu.r2.cloudflarestorage.com \
            --content-type application/json \
            --cache-control "public, max-age=86400"
        env:
          AWS_ACCESS_KEY_ID:     ${{ secrets.CLOUDFLARE_R2_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.CLOUDFLARE_R2_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION:    auto

      - name: Publish image license data
        run: |
          gh release delete image-licenses --yes 2>/dev/null || true
          gh release create image-licenses \
            --title "Image License Data ($ZIM_VERSION)" \
            --notes "Auto-generated Wikimedia Commons license data for \`$ZIM_VERSION\`.

          The Wissensfreund app downloads this file on first start to check which images may be displayed (CC0 / CC BY / CC BY-SA only).

          Updated automatically on the 1st of every month." \
            --latest=false \
            media_licenses.json
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ZIM_VERSION: ${{ env.ZIM_VERSION }}


  # ── Job: Download image shards in parallel (3 × 1/3 of all images) ───────────
  images:
    runs-on: ubuntu-latest
    needs: prepare-images
    timeout-minutes: 180
    strategy:
      fail-fast: false
      matrix:
        shard: [0, 1, 2]

    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: '3.10'

      - name: Install Python dependencies
        run: pip install requests zstandard

      - name: Download licenses artifact
        uses: actions/download-artifact@v5
        with:
          name: media-licenses

      - name: Download ZIM artifact
        uses: actions/download-artifact@v5
        with:
          name: klexikon-zim

      - name: Download image tiers — shard ${{ matrix.shard }}/3
        run: python scripts/download_images.py
        env:
          ZIM_FILE:      klexikon.zim
          LICENSES_FILE: media_licenses.json
          ZIM_VERSION:   ${{ env.ZIM_VERSION }}
          MAX_IMAGES:    ${{ inputs.max_articles || '0' }}
          COMMONS_DELAY: '0.15'
          SHARD_INDEX:   ${{ matrix.shard }}
          SHARD_COUNT:   '3'

      - name: Upload shard ${{ matrix.shard }} artifacts
        uses: actions/upload-artifact@v7
        with:
          name: images-shard-${{ matrix.shard }}
          retention-days: 1
          path: |
            images_thumb.zip
            images_standard.zip
            images_pro.zip
            images_thumb_manifest.json
            images_standard_manifest.json
            images_pro_manifest.json


  # ── Job: Merge shards and upload to Cloudflare R2 ────────────────────────────
  images-merge:
    runs-on: ubuntu-latest
    needs: images
    timeout-minutes: 90
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: '3.10'

      - name: Download shard 0
        uses: actions/download-artifact@v5
        with:
          name: images-shard-0
          path: shard-0/

      - name: Download shard 1
        uses: actions/download-artifact@v5
        with:
          name: images-shard-1
          path: shard-1/

      - name: Download shard 2
        uses: actions/download-artifact@v5
        with:
          name: images-shard-2
          path: shard-2/

      - name: Merge image shards
        run: python scripts/merge_image_shards.py
        env:
          SHARD_COUNT: '3'
          ZIM_VERSION: ${{ env.ZIM_VERSION }}

      - name: Upload image tiers to Cloudflare R2
        run: |
          R2_BASE="s3://${{ secrets.CLOUDFLARE_R2_BUCKET_NAME }}"
          ENDPOINT="https://${{ secrets.CLOUDFLARE_ACCOUNT_ID }}.eu.r2.cloudflarestorage.com"

          for TIER in thumb standard pro; do
            if [ -f "images_${TIER}_manifest.json" ]; then
              aws s3 cp "images_${TIER}_manifest.json" \
                "${R2_BASE}/images_${TIER}_manifest.json" \
                --endpoint-url "$ENDPOINT" \
                --content-type application/json \
                --cache-control "public, max-age=86400"
              echo "Uploaded images_${TIER}_manifest.json"
            fi
            if [ -f "images_${TIER}.zip" ]; then
              aws s3 cp "images_${TIER}.zip" \
                "${R2_BASE}/images_${TIER}.zip" \
                --endpoint-url "$ENDPOINT" \
                --content-type application/zip \
                --cache-control "public, max-age=86400"
              echo "Uploaded images_${TIER}.zip"
            fi
          done
        env:
          AWS_ACCESS_KEY_ID:     ${{ secrets.CLOUDFLARE_R2_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.CLOUDFLARE_R2_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION:    auto


  # ── AUDIO PIPELINE ─────────────────────────────────────────────────────────────
  #
  # prepare-audio: ZIM download + audio refs + Wikipedia discovery
  # Runtime: up to 90 min (Wikipedia API + Commons license checks)
  # Timeout ensures image pipeline is never blocked by audio.

  prepare-audio:
    runs-on: ubuntu-latest
    timeout-minutes: 90

    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: '3.10'

      - name: Install Python dependencies
        run: pip install requests zstandard

      - name: Download ZIM file
        run: |
          echo "Downloading $ZIM_URL"
          curl -fsSL -o klexikon.zim "$ZIM_URL"
          ls -lh klexikon.zim

      - name: Extract audio refs from article HTML
        run: python scripts/extract_article_audio.py
        env:
          ZIM_FILE: klexikon.zim
          MAX_ARTICLES: ${{ inputs.max_articles || '0' }}

      - name: Find Wikipedia audio files
        run: python scripts/find_wikipedia_audio.py
        env:
          ZIM_FILE:      klexikon.zim
          ZIM_VERSION:   ${{ env.ZIM_VERSION }}
          MAX_ARTICLES:  ${{ inputs.max_articles || '0' }}
          LICENSES_FILE: media_licenses.json

      - name: Upload audio refs artifact
        uses: actions/upload-artifact@v7
        with:
          name: audio-refs
          path: wikipedia_audio_refs.json
          retention-days: 1


  # ── Job: Download audio (after rate-limit reset) ──────────────────────────────
  download:
    runs-on: ubuntu-latest
    needs: prepare-audio
    timeout-minutes: 180
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: '3.10'

      - name: Install Python dependencies
        run: pip install requests

      - name: Download audio refs artifact
        uses: actions/download-artifact@v5
        with:
          name: audio-refs

      - name: Wait for Wikimedia rate-limit reset
        run: |
          echo "Warte 3600 s damit das Wikimedia-Rate-Limit nach prepare-audio zurueckgesetzt wird..."
          sleep 3600

      - name: Download audio files and build package
        run: python scripts/download_audio.py
        env:
          ZIM_VERSION: ${{ env.ZIM_VERSION }}
          REFS_FILE:   wikipedia_audio_refs.json

      - name: Upload audio package to Cloudflare R2
        run: |
          aws s3 cp audio_index.json \
            s3://${{ secrets.CLOUDFLARE_R2_BUCKET_NAME }}/audio_index.json \
            --endpoint-url https://${{ secrets.CLOUDFLARE_ACCOUNT_ID }}.eu.r2.cloudflarestorage.com \
            --content-type application/json \
            --cache-control "public, max-age=86400"
          aws s3 cp wissensfreund_audio.zip \
            s3://${{ secrets.CLOUDFLARE_R2_BUCKET_NAME }}/wissensfreund_audio.zip \
            --endpoint-url https://${{ secrets.CLOUDFLARE_ACCOUNT_ID }}.eu.r2.cloudflarestorage.com \
            --content-type application/zip \
            --cache-control "public, max-age=86400"
        env:
          AWS_ACCESS_KEY_ID:     ${{ secrets.CLOUDFLARE_R2_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.CLOUDFLARE_R2_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION:    auto

      - name: Publish audio package
        run: |
          gh release delete wissensfreund-audio --yes 2>/dev/null || true
          gh release create wissensfreund-audio \
            --title "Audio Package ($ZIM_VERSION)" \
            --notes "Wikimedia Commons audio files linked in Klexikon articles (\`$ZIM_VERSION\`).

          Files: wissensfreund_audio.zip (CC0/CC BY/CC BY-SA, max 5 MB each, max 50 MB total) and audio_index.json (article to audio file mapping).

          The Wissensfreund app downloads this package on first start for offline audio playback. Updated automatically on the 1st of every month." \
            --latest \
            wissensfreund_audio.zip audio_index.json
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ZIM_VERSION: ${{ env.ZIM_VERSION }}
